spring:
  application:
    name: hear2

  datasource:
    url: ${SPRING_DATASOURCE_URL:}
    username: ${SPRING_DATASOURCE_USERNAME:}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

app:
  file-storage:
    type: ${CHAT_STORAGE_TYPE:local}
    upload-dir: ${CHAT_UPLOAD_DIR:uploads}
  emotion:
    analysis:
      enabled: ${EMOTION_ANALYSIS_ENABLED:true}
      base-url: ${EMOTION_ANALYSIS_BASE_URL:http://localhost:8000}
      analyze-path: ${EMOTION_ANALYSIS_PATH:/analyze}
      connect-timeout-ms: ${EMOTION_ANALYSIS_CONNECT_TIMEOUT_MS:1500}
      read-timeout-ms: ${EMOTION_ANALYSIS_READ_TIMEOUT_MS:5000}
  fcm:
    enabled: ${FCM_ENABLED:false}
    credentials-path: ${FCM_CREDENTIALS_PATH:}
    project-id: ${FCM_PROJECT_ID:}
  jwt:
    # TODO: set JWT_SECRET in the deployment environment.
    secret: ${JWT_SECRET:}
    # TODO: set JWT_ACCESS_TOKEN_EXPIRATION_SECONDS in the deployment environment.
    access-token-expiration-seconds: ${JWT_ACCESS_TOKEN_EXPIRATION_SECONDS:}

cloudflare:
  r2:
    access-key: ${R2_ACCESS_KEY:}
    secret-key: ${R2_SECRET_KEY:}
    endpoint: ${R2_ENDPOINT:}
    bucket: ${R2_BUCKET:hear2-storage}
    public-url: ${R2_PUBLIC_URL:}
    region: ${R2_REGION:auto}

server:
  port: 8080
